# Memory — Public read-only; Admin riêng; nhập tộc

- **Public viewer không CRUD** — API admin chỉ trên `vu-toc-lang-chuong-admin.*` + Cloudflare Access.
- Sửa hồ sơ: modal → **Sửa hồ sơ trong Admin** hoặc footer **Quản trị** → login Access → form mở sẵn `?ma=person_code`.
- **Con gái ngoại tộc nhập họ** (vd Vũ Thị Được): `lineage_role=daughter_contributor`, `is_dinh=1`, `main`; chồng Đinh nam giữ `dinh`.
- Con chung phải có **cha + mẹ** — migration Django hay chỉ ghi mẹ → modal thiếu Thân phụ (đã vá 5 con 01.05.014–019 → Vũ Văn Khanh).
