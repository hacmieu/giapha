# Memory: Scale mặc định viewer = 0.7

- **Thời gian:** 2026-07-19 13:30
- **Phạm vi:** `drop/genealogy-viewer/public/js/genealogy-viewer.js`

## Ghi nhớ
1. **Quy ước initial view (chốt theo người dùng):** load lên `scale = 0.7` (không phải 1:1), node Tổ căn giữa ngang, cách mép trên 24px. Nằm trong listener `InitialLayoutCompleted` của `initDiagram()`.
2. Logic position dùng `d.viewportBounds.width` sau khi set scale nên đổi scale không cần sửa công thức căn giữa.
3. Cache-bust hiện tại: JS `?v=12`, CSS `?v=9`. Deploy version `7087d98c`.
