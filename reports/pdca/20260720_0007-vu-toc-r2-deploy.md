# PDCA Report — R2 + deploy staging Vũ Tộc

**Thời điểm:** 2026-07-20 00:07 (UTC+7)  
**Plan:** [../../plans/20260720_0007-vu-toc-r2-deploy.md](../../plans/20260720_0007-vu-toc-r2-deploy.md)  
**Memory:** [../../memory/20260720_0007-vu-toc-r2-deploy.md](../../memory/20260720_0007-vu-toc-r2-deploy.md)

## 1. Context

Account Hacmieu đã bật R2 (trước đó API trả `10042`). D1 đã có 397 người. Cần hoàn tất media + deploy staging.

## 2. Plan

- Tạo R2 bucket APAC.
- Upload ảnh với `--remote` (Wrangler mặc định ghi local nếu thiếu cờ).
- Deploy public/admin.
- Kiểm tra live; chưa cấu hình Cloudflare Access application.

## 3. Do

- Bucket: `vu-toc-lang-chuong-media`.
- Objects remote: `members/NgoanVV_1.jpg`, `drafts/NgoanVV_1.jpg`, placeholders.
- Deploy:
  - Public: https://vu-toc-lang-chuong-public.hacmieu.workers.dev  
    Version `8fe2ead7-4643-4a78-ae14-c8522b0f2507`
  - Admin: https://vu-toc-lang-chuong-admin.hacmieu.workers.dev  
    Version `261145eb-6580-4a76-8486-55d299cc17dc`

## 4. Check

- PASS: `GET /api/health` public → `{ok:true, mode:public}`.
- PASS: `GET /api/health` admin → `{ok:true, mode:admin}`.
- PASS: `GET /api/public/tree` → 306 người, 8 chi, village Làng Chuông.
- PASS: media `members/NgoanVV_1.jpg` → 200, 84530 bytes, `image/jpeg`.
- PASS: search `q=Vũ` → 50 kết quả (38 main + 12 external trong trang đầu).
- PASS: `POST /api/admin/people` không JWT → 401 `access_token_missing`.
- NOT RUN: Access application thật / JWT end-to-end CRUD.
- NOT RUN: port viewer Trần Tộc / form quản trị UI.

## 5. Act

1. Tạo Cloudflare Access cho hostname admin; điền `CF_ACCESS_TEAM_DOMAIN` + `CF_ACCESS_AUD`.
2. Redeploy admin với Access vars.
3. Port viewer GoJS Trần Tộc lên public API.
4. Xây form CRUD quản trị.
5. (Tuỳ chọn) nhập tay `wife_order` cho 2 cặp nhiều vợ còn thiếu.

## 6. Changed files

- `cloudflare/vu-toc-lang-chuong/README.md` (trạng thái remote).
- Plan/report/memory mới + README SSOT.

## 7. Risks & rollback

- Admin site HTML vẫn public; chỉ API ghi bị khóa JWT. Cần Access ở edge để khóa cả trang admin.
- Wrangler `r2 object put` không có `--remote` sẽ ghi local — luôn dùng `--remote` cho production.
- Rollback Worker: `wrangler rollback --env public|admin`.

## 8. Next actions

Cấu hình Cloudflare Access cho admin, rồi port viewer.
